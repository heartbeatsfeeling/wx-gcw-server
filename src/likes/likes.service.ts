import { Injectable } from '@nestjs/common'

@Injectable()
export class LikesService {
  // @Get()
  // like (
  //   @Req() request: CustomRequest,
  //   @Query('id') id: number
  // ) {
  //   return this.logsService.like(request.cookies.token, Number(id))
  // }

  // @Put()
  // unLike (
  //   @Req() request: CustomRequest,
  //   @Query('id') id: number
  // ) {
  //   return this.logsService.like(request.cookies.token, Number(id))
  // }
}
